# Render/Docker build compatibility fix

- Production build now runs `next build --webpack` rather than the Next.js 16 default Turbopack builder.
- Docker build stage sets `CI=1`.
- The npm upgrade notice is informational and is not the build failure.
- The supplied log fragment only contains Docker's final exit-code wrapper; the actual Next.js compiler error appears earlier in the Render log.
